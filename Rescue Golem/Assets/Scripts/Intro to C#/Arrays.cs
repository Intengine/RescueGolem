using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arrays : MonoBehaviour
{
    public List<int> items = new List<int>(); // list (resizable)
    public int[] backpack = new int[10]; // array (fixed size - if it's 10, then it can't be more or less than 10)

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}